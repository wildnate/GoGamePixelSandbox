plugins {
    id 'com.android.application'
}

android {
    namespace 'com.wildnate.gogame'
    compileSdk 34

    defaultConfig {
        applicationId "com.wildnate.gogame"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.aar'])
}
